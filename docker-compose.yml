services:
  finagent:
    build: .
    ports:
      - "7860:7860"
    env_file:
      - .env
    environment:
      - WEB_HOST=0.0.0.0
      - WEB_PORT=7860
    volumes:
      - ./data:/app/data
      - ./knowledge:/app/knowledge
      - ./logs:/app/logs
